import React from 'react'

const Header = ({
                    
                }) =>
(
    <header className="main">
        <h1>
            Kanban
        </h1>
    </header>
)

export default Header
